#include <stdio.h>
#include <stdlib.h>

void erastothenes(int n, int* array, int* arr_size){

}

int main(int argc, char const *argv[])
{
	int n;
	int * array, arr_size;
	*arr_size = 0;

	printf("Digite n: ");
	scanf("%d", &n);

	array = (int*) malloc(n*sizeof(int));

	erastothenes(n, array, )
	return 0;
}
